using UnityEngine;

namespace SurvivorIO
{
    // Static large tiled ground — stays fixed at world origin.
    // As the camera follows the player, different portions of the tiled
    // texture become visible, creating a natural infinite-scroll effect.
    public class InfiniteBackground : MonoBehaviour { }
}
